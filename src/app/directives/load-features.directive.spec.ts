import { LoadFeaturesDirective } from './load-features.directive';

describe('LoadFeaturesDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadFeaturesDirective();
    expect(directive).toBeTruthy();
  });
});
